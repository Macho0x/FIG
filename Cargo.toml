[workspace]
resolver = "2"
members = [
    "crates/fig-core",
    "crates/fig-usl",
    "crates/fig-exchange-sim",
    "crates/fig-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Macho0x/fig"
homepage = "https://github.com/Macho0x/fig"
description = "FIG — Unified Network Interchange Protocol"

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
# QUIC transport
quinn = "0.11"
rustls = { version = "0.23", default-features = false, features = ["ring"] }
rcgen = "0.13"
# Binary encoding
bytes = "1"
byteorder = "1"
# CBOR for extension metadata
ciborium = "0.2"
# Error handling
thiserror = "1"
anyhow = "1"
# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
# CLI
clap = { version = "4", features = ["derive"] }
# Misc
uuid = { version = "1", features = ["v4"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bitflags = "2"
# Internal crates
fig-core = { path = "crates/fig-core" }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
